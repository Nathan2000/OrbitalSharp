<Window x:Class="OrbitalSharp.Wpf.Controls.NewOrbitWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:classes="clr-namespace:OrbitalSharp.Wpf.Classes"
        xmlns:vm="clr-namespace:OrbitalSharp.Wpf.ViewModel"
        mc:Ignorable="d"
        Title="{Binding Title}"
        Height="220" Width="300"
        WindowStartupLocation="CenterOwner"
        SizeToContent="Height"
        ResizeMode="NoResize"
        d:DataContext="{d:DesignInstance Type=vm:DebugNewOrbitViewModel}">
    <Window.Resources>
        <ObjectDataProvider x:Key="Bodies" ObjectType="{x:Type classes:BodyDataProvider}" MethodName="GetAllBodies"/>
        <ObjectDataProvider x:Key="DesignFields" ObjectType="{x:Type classes:DesignTimeDataFieldProvider}" MethodName="GetSampleDataFields"/>
        <DataTemplate x:Key="DoubleTemplate">
            <TextBox Text="{Binding Value}" Width="200" />
        </DataTemplate>
        <DataTemplate x:Key="BodyTemplate">
            <ComboBox
                ItemsSource="{Binding Source={StaticResource Bodies}}"
                SelectedItem="{Binding Value}"
                DisplayMemberPath="Name"
                IsEditable="False"
                Width="200"/>
        </DataTemplate>
        <classes:FieldTemplateSelector
            x:Key="TemplateSelector"
            DoubleTemplate="{StaticResource DoubleTemplate}"
            BodyTemplate="{StaticResource BodyTemplate}"/>
    </Window.Resources>
    <StackPanel Margin="20">
        <ItemsControl ItemsSource="{Binding Fields}" d:ItemsSource="{Binding Source={StaticResource DesignFields}}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding Label}" Margin="0,5,0,5"/>
                        <ContentControl Content="{Binding}" ContentTemplateSelector="{StaticResource TemplateSelector}" ToolTip="{Binding Description}"/>
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button Content="Create" Width="80" Margin="0,0,10,0" IsDefault="True" Click="Create_Click"/>
            <Button Content="Cancel" Width="80" IsCancel="True" Click="Cancel_Click"/>
        </StackPanel>
    </StackPanel>
</Window>
